@model Coderz220.Models.ModelViews.EmployeesDepartments

@{
    ViewData["Title"] = "test";
}

<h1>test</h1>

<a class="btn btn-success my-2" asp-action="AddEmployee">Add new Employee</a>
@if (Model.Employees.Count() != 0)
{
    <table class="table table-bordered">
        <tr>
            <td>EmployeeName</td>
            <td>Salary</td>
            <td>DepartmentName</td>
            <td>operations</td>
        </tr>
        @foreach (var item in Model.Employees)
        {
            <tr>
                <td>@item.EmployeeName</td>
                <td>@item.Salary</td>
                <td>@item.Department!.DepartmentName</td>
                <td>
                    <a asp-action="Details" asp-controller="Employees" asp-route-id="@item.EmployeeId" class="btn btn-primary btn-sm">More Details</a>
                    <button type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#@(item.EmployeeName + item.EmployeeId)">
                        Delete
                    </button>
                    <!-- Modal -->
                    <div class="modal fade" id="@(item.EmployeeName+item.EmployeeId)" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="@(item.EmployeeName+item.EmployeeId)Label" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h1 class="modal-title fs-5" id="@(item.EmployeeName+item.EmployeeId)Label">Are you sure to delete @item.EmployeeName?</h1>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <div class="container-fluid">
                                        <div class="col">
                                            This will delete the Employee :
                                        </div>
                                        <div class="col">
                                            Name : @item.EmployeeName
                                        </div>
                                    </div>


                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                    <a asp-action="Remove" asp-controller="Employees" asp-route-id="@item.EmployeeId" class="btn btn-danger btn-sm">Delete</a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <a asp-action="Edit" asp-controller="Employees" asp-route-id="@item.EmployeeId" class="btn btn-warning btn-sm">Edit</a>
                </td>
            </tr>

        }
    </table>

}
else
{
    <div class="alert alert-warning">
        No Employee is Added
    </div>
}



<h1>test2</h1>

<a class="btn btn-success my-2" asp-action="CreateDepartment">Add new Department</a>
@if (Model.Departments.Count() != 0)
{
    <table class="table table-bordered">
        <tr>
            <td>DepartmentName</td>
            <td>operations</td>
        </tr>
        @foreach (var item in Model.Departments)
        {
            <tr>
                <td>@item.DepartmentName</td>
                <td>
                    <a asp-action="DetailsDepartment" asp-controller="Employees" asp-route-id="@item.DepartmentId" class="btn btn-primary btn-sm">More Details</a>
                    <button type="button" class="btn btn-danger btn-sm" data-bs-toggle="modal" data-bs-target="#@(item.DepartmentName + item.DepartmentId)">
                        Delete
                    </button>
                    <!-- Modal -->
                    <div class="modal fade" id="@(item.DepartmentName + item.DepartmentId)" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="@(item.DepartmentName + item.DepartmentId)Label" aria-hidden="true">
                        <div class="modal-dialog">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h1 class="modal-title fs-5" id="@(item.DepartmentName + item.DepartmentId)Label">Are you sure to delete @item.DepartmentName?</h1>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <div class="container-fluid">
                                        <div class="col">
                                            This will delete the Employee :
                                        </div>
                                        <div class="col">
                                            Name : @item.DepartmentName
                                        </div>
                                    </div>


                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                    <a asp-action="RemoveDepartment" asp-controller="Employees" asp-route-id="@item.DepartmentId" class="btn btn-danger btn-sm">Delete</a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <a asp-action="EditDepartment" asp-controller="Employees" asp-route-id="@item.DepartmentId" class="btn btn-warning btn-sm">Edit</a>
                </td>
            </tr>

        }
    </table>

}
else
{
    <div class="alert alert-warning">
        No Employee is Added
    </div>
}